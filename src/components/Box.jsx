import styled from 'styled-components'
import { typography, space, color, border, layout, flexbox } from 'styled-system'

export const Box = styled('div')(
  typography,
  space,
  color,
  border,
  layout,
  flexbox,
)